alias kubectl="microk8s.kubectl"

# emacs
alias doom="emacs --with-profile doom"
alias prelude="emacs --with-profile prelude"

# docker
alias dockerkill="docker ps --all --quiet | xargs docker rm --force"

alias fzf="fzf-slowday.fzf"
alias path='echo $PATH | tr ":" "\n"'
alias sl="ls"
alias mv="mv -i"
alias hs='history -a; history -n'
alias bot='cd $(dirname $(find . | tail -1))'
alias clr='cd ~/ && clear'
